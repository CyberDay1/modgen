import * as React from 'react';
import { createRoot } from 'react-dom/client';

const App = () => <div>OpenAI Mod Generator MVP</div>;

const root = createRoot(document.getElementById('root')!);
root.render(<App />);
